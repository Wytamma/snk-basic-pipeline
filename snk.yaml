cli: # snk will use these annotations to improve the cli
  genome:
    type: Path
    help: "Path to the reference genome."
    short: g
  sample:
    type: List
    help: "Samples to map to the reference genome."
    short: s
  samples_dir:
    type: Path
    help: "Directory of samples."
    required: False
